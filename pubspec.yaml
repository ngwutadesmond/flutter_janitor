name: flutter_janitor
description: Conservative Flutter cleanup CLI for unused assets and dependencies with reports, backups, and undo.
version: 0.1.0
repository: https://github.com/example/flutter_janitor
homepage: https://github.com/example/flutter_janitor
issue_tracker: https://github.com/example/flutter_janitor/issues
license: MIT

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  args: ^2.7.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3

executables:
  flutter_janitor: flutter_janitor
